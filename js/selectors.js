function paragraphSelector() {
  const list = document.querySelectorAll('p');
  return list;
}

function lastImageSelector() {
  const lastImg = document.querySelectorAll('img')
  return lastImg[lastImg.length];
}